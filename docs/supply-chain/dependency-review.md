# Dependency Review Notes

Dependency changes should be easy to audit.

- Prefer maintained libraries with clear release history.
- Review transitive dependency changes before merge.
- Keep build plugins pinned through normal version updates.

Review cadence: update when the related workflow changes.
