# Predicate Catalog

This catalog keeps route predicate behavior discoverable.

- Host based predicates choose by request host.
- Path based predicates choose by normalized path.
- Header based predicates must avoid sensitive values.

Review cadence: update when the related workflow changes.
